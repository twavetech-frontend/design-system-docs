import { Button } from '../../../components/Button'
import { ComponentPreview, PropsTable, StateLabel } from '../../../components/ComponentPreview'

# Close Button

모달, 토스트, 배너 등을 닫는 데 사용하는 X 버튼입니다.

Figma 컴포넌트: `Buttons/Button close X`

---

## Size

3가지 사이즈를 지원합니다: `sm` `md` `lg`.

<ComponentPreview code={`<Button variant="close" size="sm" iconOnly={true}>✕</Button>
<Button variant="close" size="md" iconOnly={true}>✕</Button>
<Button variant="close" size="lg" iconOnly={true}>✕</Button>`}>
  <Button variant="close" size="sm" iconOnly={true}>✕</Button>
  <Button variant="close" size="md" iconOnly={true}>✕</Button>
  <Button variant="close" size="lg" iconOnly={true}>✕</Button>
</ComponentPreview>

---

## States

<ComponentPreview label="Light background" code={`<Button variant="close" size="md" iconOnly={true}>✕</Button>
<Button variant="close" size="md" state="hover" iconOnly={true}>✕</Button>
<Button variant="close" size="md" state="focused" iconOnly={true}>✕</Button>`}>
  <Button variant="close" size="md" iconOnly={true}>✕</Button>
  <Button variant="close" size="md" state="hover" iconOnly={true}>✕</Button>
  <Button variant="close" size="md" state="focused" iconOnly={true}>✕</Button>
</ComponentPreview>

---

## Props

<PropsTable data={[
  { name: 'variant', type: "close", default: "close", description: 'Close 버튼' },
  { name: 'size', type: "sm | md | lg", default: "md", description: '버튼 크기' },
  { name: 'iconOnly', type: 'boolean', default: 'true', description: 'Close 버튼은 항상 아이콘 전용으로 사용합니다' },
]} />

---

## Figma Variants

| Property | Values |
| --- | --- |
| **Size** | `sm` `md` `lg` |
| **State** | `Default` `Hover` `Focused` |
